# --- Paper + backtest phases (run on Python 3.12–3.14) ---
httpx>=0.27          # HTTP client for public Data/Gamma APIs
websockets>=12.0     # real-time CLOB market data (later)
pandas>=2.2          # data wrangling / backtest reports
pydantic>=2.7        # typed models
pydantic-settings>=2.3  # env-driven config
pyyaml>=6.0          # leaders / scoring config
python-dotenv>=1.0   # .env loading
tenacity>=8.3        # retry/backoff for API calls

# --- Dev / test ---
pytest>=8.2
pytest-asyncio>=0.23

# --- LIVE ONLY (install later, in a Python 3.12 venv) ---
# py-clob-client>=0.34   # Polymarket order placement — Phase 6, gated
